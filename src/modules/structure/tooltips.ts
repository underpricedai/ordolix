/**
 * Tooltip content dictionary for the Structure module.
 * @module structure-tooltips
 */

const tooltips = {
  structure:
    "A hierarchical tree view of issues grouped by any field",
  groupBy:
    "Choose how to group issues: by epic, assignee, priority, status, or sprint",
  view: "A saved structure configuration that can be shared with your team",
} as const;

export default tooltips;
